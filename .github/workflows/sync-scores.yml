name: Sync Scores

on:
  schedule:
    - cron: '*/30 * * * *'  # toutes les 30 minutes
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync World Cup scores to Airtable
        env:
          FOOTBALL_TOKEN: ${{ secrets.FOOTBALL_DATA_TOKEN }}
          AIRTABLE_TOKEN: ${{ secrets.AIRTABLE_TOKEN }}
          BASE_ID: app6kZ7JVeTWarmFR
        run: |
          node << 'EOF'
          const https = require('https');

          function norm(s) {
            const aliases = {
              'ivory coast': 'cotedivoire',
              'cote divoire': 'cotedivoire',
              'cape verde islands': 'capvert',
              'cape verde': 'capvert',
              'congo dr': 'rdcongo',
              'dr congo': 'rdcongo',
              'scotland': 'ecosse',
              'france': 'france',
              'spain': 'espagne',
              'germany': 'allemagne',
              'netherlands': 'paysbas',
              'brazil': 'bresil',
              'morocco': 'maroc',
              'haiti': 'haiti',
              'australia': 'australie',
              'turkey': 'turquie',
              'turkiye': 'turquie',
              'switzerland': 'suisse',
              'sweden': 'suede',
              'tunisia': 'tunisie',
              'belgium': 'belgique',
              'egypt': 'egypte',
              'iran': 'iran',
              'new zealand': 'nouvellezeland',
              'norway': 'norvege',
              'senegal': 'senegal',
              'argentina': 'argentine',
              'algeria': 'algerie',
              'austria': 'autriche',
              'jordan': 'jordanie',
              'portugal': 'portugal',
              'england': 'angleterre',
              'croatia': 'croatie',
              'ghana': 'ghana',
              'panama': 'panama',
              'uzbekistan': 'ouzbekistan',
              'colombia': 'colombie',
              'ecuador': 'equateur',
              'curacao': 'curacao',
              'japan': 'japon',
              'saudi arabia': 'arabiesaoudite',
              'uruguay': 'uruguay',
              'iraq': 'irak',
              'south africa': 'afriquedusud',
              'south korea': 'coreedusud',
              'czechia': 'reptcheque',
              'czech republic': 'reptcheque',
              'canada': 'canada',
              'qatar': 'qatar',
              'mexico': 'mexique',
              'usa': 'etatsuni',
              'united states': 'etatsuni',
              'paraguay': 'paraguay',
              'bosnia and herzegovina': 'bosnieherzegovine',
              'bosnia-herzegovina': 'bosnieherzegovine',
              'bosnia herzegovina': 'bosnieherzegovine',
            };
            let r = s.toLowerCase().trim();
            for (const [k,v] of Object.entries(aliases).sort((a,b)=>b[0].length-a[0].length)) {
              if (r === k || r.includes(k)) { r = r.replace(k, v); break; }
            }
            return r
              .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
              .replace(/[^a-z0-9]/g, '');
          }

          const MATCHES = {
            m001: ['Mexico', 'South Africa'],
            m002: ['South Korea', 'Czechia'],
            m003: ['Czechia', 'South Africa'],
            m004: ['Mexico', 'South Korea'],
            m005: ['South Africa', 'South Korea'],
            m006: ['Mexico', 'Czechia'],
            m007: ['Canada', 'Bosnia and Herzegovina'],
            m008: ['Qatar', 'Switzerland'],
            m009: ['Switzerland', 'Bosnia and Herzegovina'],
            m010: ['Canada', 'Qatar'],
            m011: ['Bosnia and Herzegovina', 'Qatar'],
            m012: ['Switzerland', 'Canada'],
            m013: ['Brazil', 'Morocco'],
            m014: ['Haiti', 'Scotland'],
            m015: ['Scotland', 'Morocco'],
            m016: ['Brazil', 'Haiti'],
            m017: ['Scotland', 'Brazil'],
            m018: ['Morocco', 'Haiti'],
            m019: ['United States', 'Paraguay'],
            m020: ['Australia', 'Turkey'],
            m021: ['United States', 'Australia'],
            m022: ['Turkey', 'Paraguay'],
            m023: ['Turkey', 'United States'],
            m024: ['Paraguay', 'Australia'],
            m025: ['Germany', 'Curacao'],
            m026: ['Cote dIvoire', 'Ecuador'],
            m027: ['Germany', 'Cote dIvoire'],
            m028: ['Ecuador', 'Curacao'],
            m029: ['Ecuador', 'Germany'],
            m030: ['Curacao', 'Cote dIvoire'],
            m031: ['Netherlands', 'Japan'],
            m032: ['Sweden', 'Tunisia'],
            m033: ['Netherlands', 'Sweden'],
            m034: ['Tunisia', 'Japan'],
            m035: ['Japan', 'Sweden'],
            m036: ['Tunisia', 'Netherlands'],
            m037: ['Belgium', 'Egypt'],
            m038: ['Iran', 'New Zealand'],
            m039: ['Belgium', 'Iran'],
            m040: ['New Zealand', 'Egypt'],
            m041: ['Egypt', 'Iran'],
            m042: ['New Zealand', 'Belgium'],
            m043: ['Spain', 'Cape Verde'],
            m044: ['Saudi Arabia', 'Uruguay'],
            m045: ['Spain', 'Saudi Arabia'],
            m046: ['Uruguay', 'Cape Verde'],
            m047: ['Cape Verde', 'Saudi Arabia'],
            m048: ['Uruguay', 'Spain'],
            m049: ['France', 'Senegal'],
            m050: ['Iraq', 'Norway'],
            m051: ['France', 'Iraq'],
            m052: ['Norway', 'Senegal'],
            m053: ['Norway', 'France'],
            m054: ['Senegal', 'Iraq'],
            m055: ['Argentina', 'Algeria'],
            m056: ['Austria', 'Jordan'],
            m057: ['Argentina', 'Austria'],
            m058: ['Jordan', 'Algeria'],
            m059: ['Algeria', 'Austria'],
            m060: ['Jordan', 'Argentina'],
            m061: ['Portugal', 'DR Congo'],
            m062: ['Uzbekistan', 'Colombia'],
            m063: ['Portugal', 'Uzbekistan'],
            m064: ['Colombia', 'DR Congo'],
            m065: ['Colombia', 'Portugal'],
            m066: ['DR Congo', 'Uzbekistan'],
            m067: ['England', 'Croatia'],
            m068: ['Ghana', 'Panama'],
            m069: ['England', 'Ghana'],
            m070: ['Panama', 'Croatia'],
            m071: ['Panama', 'England'],
            m072: ['Croatia', 'Ghana'],
            m073: ['South Africa', 'Canada'],
            m074: ['Brazil', 'Japan'],
            m075: ['Germany', 'Paraguay'],
            m076: ['Netherlands', 'Morocco'],
            m077: ['Ivory Coast', 'Norway'],
            m078: ['France', 'Sweden'],
            m079: ['Mexico', 'Ecuador'],
            m080: ['England', 'DR Congo'],
            m081: ['Belgium', 'Senegal'],
            m082: ['United States', 'Bosnia and Herzegovina'],
            m083: ['Spain', 'Austria'],
            m084: ['Portugal', 'Croatia'],
            m085: ['Switzerland', 'Algeria'],
            m086: ['Australia', 'Egypt'],
            m087: ['Argentina', 'Cape Verde'],
            m088: ['Colombia', 'Ghana'],
          };

          const INDEX = {};
          for (const [mid, [t1, t2]] of Object.entries(MATCHES)) {
            INDEX[norm(t1) + '_' + norm(t2)] = { mid, swapped: false };
            INDEX[norm(t2) + '_' + norm(t1)] = { mid, swapped: true };
          }

          function fetch(url, headers) {
            return new Promise((resolve, reject) => {
              const opts = new URL(url);
              https.get({ hostname: opts.hostname, path: opts.pathname + opts.search, headers }, res => {
                let data = '';
                res.on('data', chunk => data += chunk);
                res.on('end', () => {
                  try { resolve({ status: res.statusCode, body: JSON.parse(data) }); }
                  catch (e) { resolve({ status: res.statusCode, body: data }); }
                });
              }).on('error', reject);
            });
          }

          async function atFetch(method, path, body) {
            return new Promise((resolve, reject) => {
              const data = body ? JSON.stringify(body) : null;
              const opts = {
                hostname: 'api.airtable.com',
                path: `/v0/${process.env.BASE_ID}/${path}`,
                method,
                headers: {
                  'Authorization': `Bearer ${process.env.AIRTABLE_TOKEN}`,
                  'Content-Type': 'application/json',
                  ...(data ? { 'Content-Length': Buffer.byteLength(data) } : {})
                }
              };
              const req = https.request(opts, res => {
                let d = '';
                res.on('data', chunk => d += chunk);
                res.on('end', () => { try { resolve(JSON.parse(d)); } catch(e){ resolve({}); } });
              });
              req.on('error', reject);
              if (data) req.write(data);
              req.end();
            });
          }

          function sleep(ms){ return new Promise(r=>setTimeout(r,ms)); }

          async function main() {
            console.log('Fetching World Cup matches from football-data.org...');

            const res = await fetch(
              'https://api.football-data.org/v4/competitions/2000/matches?season=2026',
              { 'X-Auth-Token': process.env.FOOTBALL_TOKEN }
            );

            if (res.status !== 200) {
              console.error('API error:', res.status, JSON.stringify(res.body).slice(0,500));
              process.exit(1);
            }

            const matches = (res.body.matches || []).filter(m => m.status === 'FINISHED');
            console.log(`Found ${matches.length} finished matches from API`);

            const existing = await atFetch('GET', 'Resultats?pageSize=100');
            const existingMap = {};
            for (const r of (existing.records || [])) {
              existingMap[r.fields.MatchId] = { id: r.id, s1: r.fields.Score1, s2: r.fields.Score2 };
            }

            let pronos = [], off = '';
            do {
              const d = await atFetch('GET', `Pronostics?pageSize=100${off ? '&offset=' + off : ''}`);
              pronos = pronos.concat(d.records || []);
              off = d.offset || '';
            } while (off);
            console.log(`Found ${pronos.length} pronostics`);

            let updated = 0, created = 0, pointsUpdated = 0, matched = 0;

            for (const match of matches) {
              const home = match.homeTeam?.name || match.homeTeam?.shortName || '';
              const away = match.awayTeam?.name || match.awayTeam?.shortName || '';
              const key = norm(home) + '_' + norm(away);
              const entry = INDEX[key];

              if (!entry) {
                console.log(`No mapping for: "${home}" vs "${away}" (key=${key})`);
                continue;
              }
              matched++;

              const score1 = match.score?.fullTime?.home;
              const score2 = match.score?.fullTime?.away;
              if (score1 === null || score1 === undefined || score2 === null || score2 === undefined) continue;

              const s1 = entry.swapped ? score2 : score1;
              const s2 = entry.swapped ? score1 : score2;
              const matchId = entry.mid;

              const prev = existingMap[matchId];
              if (prev && prev.s1 === s1 && prev.s2 === s2) continue;

              console.log(`${matchId}: ${home} ${s1}-${s2} ${away}`);

              if (prev) {
                await atFetch('PATCH', `Resultats/${prev.id}`, {
                  fields: { MatchId: matchId, Score1: s1, Score2: s2 }
                });
                updated++;
              } else {
                const created_rec = await atFetch('POST', 'Resultats', {
                  records: [{ fields: { MatchId: matchId, Score1: s1, Score2: s2 } }]
                });
                existingMap[matchId] = { id: created_rec.records?.[0]?.id, s1, s2 };
                created++;
              }

              await sleep(250);

              const matchPronos = pronos.filter(p => p.fields.MatchId === matchId);
              for (const prono of matchPronos) {
                const ps1 = prono.fields.Score1; const ps2 = prono.fields.Score2;
                const exactScore = ps1 === s1 && ps2 === s2;
                const correctResult = (s1 > s2 && ps1 > ps2) || (s1 < s2 && ps1 < ps2) || (s1 === s2 && ps1 === ps2);
                const pts = correctResult ? (exactScore ? 4 : 3) : 0;
                if (prono.fields.Points !== pts) {
                  await atFetch('PATCH', `Pronostics/${prono.id}`, { fields: { Points: pts } });
                  pointsUpdated++;
                  await sleep(250);
                }
              }
            }

            console.log(`Done! Matched: ${matched}/${matches.length}, Created: ${created}, Updated: ${updated}, Points updated: ${pointsUpdated}`);
          }

          main().catch(err => { console.error(err); process.exit(1); });
          EOF
